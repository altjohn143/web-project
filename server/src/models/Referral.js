const mongoose = require('mongoose');
module.exports = mongoose.model('Referral', new mongoose.Schema({ encounter: { type: mongoose.Schema.Types.ObjectId, ref: 'Encounter' }, patient: { type: mongoose.Schema.Types.ObjectId, ref: 'Patient', required: true }, referringDoctor: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, destination: { type: String, required: true, trim: true }, referralType: { type: String, enum: ['Doctor', 'Specialist', 'Hospital', 'Laboratory'], required: true }, reason: { type: String, required: true, trim: true }, status: { type: String, enum: ['Pending', 'Sent', 'Accepted', 'Completed', 'Cancelled'], default: 'Pending' } }, { timestamps: true }));
