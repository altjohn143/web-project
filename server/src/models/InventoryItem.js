const mongoose = require('mongoose');
const movementSchema = new mongoose.Schema({ type: { type: String, enum: ['Received', 'Used', 'Adjusted'], required: true }, quantity: { type: Number, required: true }, note: String, recordedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, date: { type: Date, default: Date.now } }, { _id: false });
module.exports = mongoose.model('InventoryItem', new mongoose.Schema({ name: { type: String, required: true, trim: true }, category: { type: String, enum: ['Medicine', 'Laboratory', 'Medical supply'], required: true }, sku: { type: String, required: true, unique: true, trim: true }, quantity: { type: Number, default: 0, min: 0 }, unit: { type: String, required: true, trim: true }, reorderLevel: { type: Number, default: 5, min: 0 }, expiryDate: Date, movements: [movementSchema], createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true } }, { timestamps: true }));
