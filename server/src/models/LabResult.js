const mongoose = require('mongoose');
module.exports = mongoose.model('LabResult', new mongoose.Schema({ patient: { type: mongoose.Schema.Types.ObjectId, ref: 'Patient', required: true }, doctor: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, testName: { type: String, required: true, trim: true, maxlength: 150 }, requestedDate: { type: Date, required: true }, status: { type: String, enum: ['Requested', 'In Progress', 'Completed', 'Cancelled'], default: 'Requested' }, result: { type: String, trim: true, maxlength: 3000 }, notes: { type: String, trim: true, maxlength: 2000 }, createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true } }, { timestamps: true }));
