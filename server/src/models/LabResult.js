const mongoose = require('mongoose');
module.exports = mongoose.model('LabResult', new mongoose.Schema({ patient: { type: mongoose.Schema.Types.ObjectId, ref: 'Patient', required: true }, doctor: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, encounter: { type: mongoose.Schema.Types.ObjectId, ref: 'Encounter' }, testName: { type: String, required: true, trim: true, maxlength: 150 }, requestedDate: { type: Date, required: true }, status: { type: String, enum: ['Requested', 'Sample collected', 'Processing', 'Completed', 'Cancelled'], default: 'Requested' }, result: { type: String, trim: true, maxlength: 3000 }, notes: { type: String, trim: true, maxlength: 2000 }, reviewedByDoctorAt: Date, fee: { type: Number, default: 0, min: 0 }, createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true } }, { timestamps: true }));
